# tools

Placeholder only. No binaries are bundled in v0.0.1.

Runtime tools are intended to live in MediaHub's central `tools/` directory,
exactly like the tools used by the other plugins.

Prepared IDs:
- ffmpeg
- ffprobe
- mediainfo
- chromaprint_fpcalc
- mp3tag
